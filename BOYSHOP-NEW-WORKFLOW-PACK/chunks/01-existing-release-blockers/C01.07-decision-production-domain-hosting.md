# DECISION — Authorize a production domain and hosting target

Run agent/model routing only to analyze the decision; do not execute implementation.

## Routing
Routing: run mandatory agent-aware preflight before execution.
Run mandatory `ROUTING-PREFLIGHT.md` immediately before this chunk.
Capability preference: analysis/decision support (no implementation).

## State

### Verified facts
- Deployment configs exist and are Git-suitable: `netlify.toml`, `wrangler.jsonc`, `open-next.config.ts`, `next.config.mjs` (C00.10 Cloudflare evidence audit — deploy config clean).
- No production domain has been chosen or authorized (GO-LIVE blocker: "production domain/hosting").
- Cloudflare artifacts (`.wrangler/`, `.open-next/`) verified git-ignored — local builds do not leak deploy state into commits.
- No live deploy target is bound to this repo yet; nothing is live.

### Available choices
1. **Authorize a domain + hosting provider** — CEO names the production hostname and picks the host (Cloudflare Workers/Pages via existing config, or Netlify via `netlify.toml`, or another). Then a later phase-22 chunk provisions DNS/TLS/env under that approval.
2. **Defer** — release stays blocked for any public URL; acceptable only while go-live is not scheduled.
3. **Use a temporary/staging hostname first** — lower-risk path; still needs CEO to name the staging host.

### Consequences
- Choice 1: unblocks phase 22 production prep and phase 23 go-live smoke tests; still gated by live payment (C01.06) and full-tree commit (C01.05) if not already done.
- Choice 2: no production URL exists; GO-LIVE checklist cannot complete.
- Choice 3: staging validates the pipeline without committing the final brand domain.

### Approval needed
CEO explicit choice among 1–3, plus for choice 1 or 3: the exact hostname and hosting provider.

## Out of scope
- Provisioning DNS, TLS, or deploy credentials inside this decision chunk.
- Any live deploy.

Set `NEXT.md` to the next unelected chunk after this decision is recorded (or to this decision again if unresolved) and STOP.
