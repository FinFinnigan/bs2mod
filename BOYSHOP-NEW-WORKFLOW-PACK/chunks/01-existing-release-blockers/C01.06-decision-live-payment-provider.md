# DECISION — Authorize a live payment provider

Run agent/model routing only to analyze the decision; do not execute implementation.

## Routing
Routing: run mandatory agent-aware preflight before execution.
Run mandatory `ROUTING-PREFLIGHT.md` immediately before this chunk.
Capability preference: analysis/decision support (no implementation).

## State

### Verified facts
- Provider-neutral payment layer: `src/lib/backend/payments/` — port + registry + store/service.
- **Mock** adapter is the verified default and the only provider proven end-to-end.
- **Mollie** adapter exists, test-mode only (`test_*` keys); live mode fail-closed unless `MOLLIE_ALLOW_LIVE=true` (CEO authorization required — see `DECISION-GATES.md` and `MOLLIE-VERIFICATION.md`).
- Stripe appears only as comments/placeholders in `.env.example` — no Stripe adapter is verified.
- No live provider has been chosen or authorized (GO-LIVE blocker: "No live payment provider").
- Full-tree commit (C01.05) should land before or independently of this decision; this decision does not unblock itself by waiting.

### Available choices
1. **Authorize Mollie live** — set real `MOLLIE_API_KEY` (`live_*`) + `MOLLIE_WEBHOOK_SECRET` + `MOLLIE_ALLOW_LIVE=true` in production env only; run live E2E payment test. Requires CEO.
2. **Stay mock-only for now** — release remains blocked for real payments; acceptable only if go-live does not take real money yet (it should not).
3. **Add/choose another provider** (e.g. Stripe) — new adapter work; material new provider commitment; out of scope for phase 01 chunk execution — would need its own recon + chunks.

### Consequences
- Choice 1: production can accept real payments after live E2E verification; still gated by domain/hosting (C01.07) and release phases 21–23.
- Choice 2: no revenue path; release audit (phase 21) will keep this blocker open.
- Choice 3: delays release; new dependency/provider commitment gate.

### Approval needed
CEO explicit choice among 1–3, and for choice 1 separately: production secret provisioning (never in repo) + `MOLLIE_ALLOW_LIVE=true` authorization.

## Out of scope
- Implementing any choice inside this decision chunk.
- Setting live keys anywhere in the repository.

Set `NEXT.md` to the next unelected chunk after this decision is recorded (or to this decision again if unresolved) and STOP.
