# DECISION — Authorize a live database migration

Run agent/model routing only to analyze the decision; do not execute implementation.

## Routing
Routing: run mandatory agent-aware preflight before execution.
Run mandatory `ROUTING-PREFLIGHT.md` immediately before this chunk.
Capability preference: analysis/decision support (no implementation).

## State

### Verified facts
- Project uses Drizzle ORM (`drizzle.config.ts`) against a Neon Postgres database (Neon skill/MCP wired in this environment).
- Migration tooling is local config only — no live database schema has been applied under CEO authorization.
- No live DB migration has been authorized (GO-LIVE blocker + hard workflow rule: live DB migration forbidden without CEO authorization; pack `WORKFLOW-RULES.md` and chunk rules both enforce this).
- `.env` / `.env.local` / `.dev.vars` are absent from the tree; no `DATABASE_URL` is stored in the repo (only `.env.example` placeholders).

### Available choices
1. **Authorize a live migration** — CEO approves applying the Drizzle schema/migrations to a named production Neon branch. Then a later phase-22 chunk runs it under that approval with a backup/branch snapshot first.
2. **Stay on a dev/branch database** — production data path remains blocked; acceptable only while go-live is deferred.
3. **Defer the decision** — nothing migrates now; blocker remains open on the GO-LIVE list.

### Consequences
- Choice 1: production schema becomes real; requires a fresh Neon branch snapshot before applying, and must not run before the full-tree commit (C01.05) so the schema definition is committed first.
- Choice 2: app cannot persist real orders/customers in production.
- Choice 3: no change — blocker stays OPEN.

### Approval needed
CEO explicit choice among 1–3; for choice 1 also: target project/branch name and confirmation that a restore snapshot exists first.

## Out of scope
- Running `drizzle-kit push`/`migrate` or any SQL against a live branch inside this decision chunk.
- Creating or deleting Neon branches here.

Set `NEXT.md` to the next unelected chunk after this decision is recorded (or to this decision again if unresolved) and STOP.
