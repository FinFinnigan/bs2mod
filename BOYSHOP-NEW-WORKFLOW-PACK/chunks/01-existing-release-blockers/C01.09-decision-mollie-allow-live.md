# DECISION — Authorize `MOLLIE_ALLOW_LIVE=true`

Run agent/model routing only to analyze the decision; do not execute implementation.

## Routing
Routing: run mandatory agent-aware preflight before execution.
Run mandatory `ROUTING-PREFLIGHT.md` immediately before this chunk.
Capability preference: analysis/decision support (no implementation).

## State

### Verified facts
- The Mollie adapter fail-closes out of live mode unless `MOLLIE_ALLOW_LIVE=true` is set in the runtime environment (verified in `src/lib/backend/payments/adapters/mollie.ts` + tests; documented in `MOLLIE-VERIFICATION.md`).
- `.env.example` ships `MOLLIE_ALLOW_LIVE` commented out — correct; the flag must never default on.
- This is a distinct gate from choosing a provider (C01.06): even after choosing Mollie live, this flag is a separate CEO authorization before any live charge is possible.
- Live keys (`live_*` API key + webhook secret) are production secrets — they go only in the hosting provider's secret store, never in the repo.
- Current mode is test-only (`test_*`); 292/292 tests and the mock default are unaffected by this decision.

### Available choices
1. **Authorize the flag for production only** — CEO approves setting `MOLLIE_ALLOW_LIVE=true` in the production environment (not local, not CI) once real `live_*` keys are provisioned; then run one live E2E payment test.
2. **Keep fail-closed** — Mollie stays test-mode forever until a later authorization; no real charges possible.
3. **Do not use Mollie live at all** — ties back to C01.06 choice 3 (different provider); this flag becomes irrelevant.

### Consequences
- Choice 1: real card payments become possible; a failed live E2E test must reopen the blocker. Depends on C01.06 = Mollie live and C01.07 = hosting where secrets live.
- Choice 2: release remains payment-blocked by design (safe default).
- Choice 3: pick a different provider first via C01.06; do not set this flag.

### Approval needed
CEO explicit choice among 1–3; for choice 1: confirmation that production secrets are provisioned in the host's secret store first (repo stays clean).

## Out of scope
- Setting `MOLLIE_ALLOW_LIVE=true` anywhere (repo, local env, CI) inside this decision chunk.
- Creating or pasting live Mollie keys.

Set `NEXT.md` to the next unelected chunk after this decision is recorded (or to this decision again if unresolved) and STOP.
