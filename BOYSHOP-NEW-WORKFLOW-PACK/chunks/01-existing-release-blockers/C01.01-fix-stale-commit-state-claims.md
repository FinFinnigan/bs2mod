# C01.01 — Fix stale HEAD / "no commit" claims in evidence docs

## Routing
Routing: run mandatory agent-aware preflight before execution.
Run mandatory `ROUTING-PREFLIGHT.md` immediately before execution.
Do not hardcode a named agent here.
Capability preference: implementation (documentation correction against live git truth).

## Outcome
All four evidence docs (`GO-LIVE.md`, `HANDOFF.md`, `PROJECT-STATE.md`, `CHANGELOG.md`) stop claiming that no commit of `BS2Mod-site/` exists and stop citing stale HEAD `d7ee42bf`, and instead record verified repo truth from live git at execution time.

## Scope
Exactly these files under `docs/evidence/`:
- `GO-LIVE.md` — header block (lines ~3–8), §2 first `BLOCKER` bullet (~33–35), §17 rollback note (~152–153), §18 checklist "Committed repository state" item (~162–163), §19 blocker table row 1 (~176), §20 first checkbox (~189), footer "Last verified" if stale.
- `HANDOFF.md` — status header (lines ~3–7), §1 paragraph (lines ~11–14, version wording fine, commit claims not), §4 first bullet (~52–54), §6 acceptance pending note (~79–80).
- `PROJECT-STATE.md` — status header (lines ~3–8), §1 "Repository truth" (lines ~10–17), §7 blocker table row 1 (~82), footer (~100–105). Do NOT touch §8 next-step here — that is C01.03's concern.
- `CHANGELOG.md` — status header (lines ~3–9), "How to read" bullets (~13–15), `[Unreleased]` intro if it repeats the no-commit claim (~23), §"Changed — phase 00" first bullet (~72–74), footer (~104–110).

Correct facts to record (verify each against live git immediately before editing — repository truth beats this prompt):
- Commit `d837b0aa` ("BS2Mod: docs/evidence C00.1-C00.12 closeout") committed exactly the five evidence docs under `BS2Mod-site/docs/evidence/`.
- The rest of `BS2Mod-site/` remains untracked (15 untracked paths: `src/`, `package.json`, `package-lock.json`, configs, `scripts/`, pack, etc. — re-run `git status --short -- .` for the live count).
- Current `main` HEAD must be re-read with `git rev-parse --short HEAD` at execution time (it was `d7ee42bf` when docs were written, then `d837b0aa`, and has since advanced past unrelated commits — do not hardcode a HEAD from memory).
- Release verdict stays **PRE-LAUNCH — RELEASE BLOCKED**: full-tree commit still CEO-gated; phases 21–23 still QUEUED.
- Canonical version remains `0.11.1`.

Wording guidance: replace absolute falsehoods ("no commit of `BS2Mod-site/` exists") with precise partial truth ("evidence docs committed at `d837b0aa`; the remaining tree is untracked pending the CEO-approved full-tree commit"). Update stale HEAD citations to the live HEAD or to a phrase that does not rot ("HEAD advances with unrelated `C:\dev` commits; the BS2Mod evidence commit is `d837b0aa`").

## Out of scope
- `MOLLIE-VERIFICATION.md` (C01.02).
- `PROJECT-STATE.md` §8 next-step (C01.03).
- Any source, config, or test file.
- Any git add/commit.
- Inventing release versions or marking blockers closed that remain open.

## Implementation
Smallest safe edit: correct only the false/stale claims listed above; preserve document structure, status banners, and all still-true content.

## Verification
- `grep -n "d7ee42bf\|no commit of" docs/evidence/GO-LIVE.md docs/evidence/HANDOFF.md docs/evidence/PROJECT-STATE.md docs/evidence/CHANGELOG.md` → no stale-claim matches remain (historical/quoted mentions of prior HEAD only if explicitly framed as historical).
- Each edited doc still says `RELEASE BLOCKED` / `PRE-LAUNCH`.
- `git diff -- docs/evidence/` shows changes only in the four named files.
- Live cross-check: `git log --oneline -1 -- BS2Mod-site/docs/evidence/` confirms `d837b0aa` still holds; `git status --short -- .` untracked count matches what the docs claim.


## Permanent execution rules

- Execute exactly **one tiny chunk** per run.
- One chunk = one concern, one small directly related file group, one independently verifiable behavior.
- Before every chunk launches, run the mandatory **agent + model routing preflight** from `ROUTING-PREFLIGHT.md`.
- Consider **all currently installed OMO agents** before choosing the executor. Never assume the installed-agent set is unchanged.
- Agent choice and model choice are separate decisions.
- Never silently switch model/provider or broaden scope because a stronger agent/model exists.
- `NEXT.md` is the only current execution entry.
- Repository truth beats stale documentation.
- `MASTER-SPEC.md` is stable product truth.
- `CURRENT-STATE.md` is concise live repo truth.
- `IMPLEMENTATION-MAP.md` is the dynamic route map.
- Future implementation chunks are generated from repository evidence, not guessed in advance.
- Preserve the modular/no-code landing-to-checkout architecture.
- Preserve the current BoyShop design as the first reusable template.
- No test deletion or test weakening merely to get green.
- No broad release audit unless the active chunk specifically authorizes it.
- No live DB migration without CEO authorization.
- No `MOLLIE_ALLOW_LIVE=true` without CEO authorization.
- Never commit secrets.
- Never use `git add -A`.
- No `as any`, `@ts-ignore`, `@ts-expect-error`, or empty catch blocks.
- No new npm dependencies unless explicitly authorized by the active chunk.
- Never silently include unrelated visual work.
- Git add/commit/tag/push require explicit user approval unless already authorized for that exact scope.
- Implementation/verification happen first; project-documentation sync happens once at closeout where applicable.
- If unrelated issues appear, report `OUT OF SCOPE — NOT CHANGED`.
- Re-evaluate the remaining plan after every chunk; skip, split, insert, or reorder future chunks when repo evidence warrants it.
- Rewrite `NEXT.md` to exactly one next chunk, then STOP.


## Required completion response

### Routing preflight
- Installed OMO agents considered: <all discovered agents>
- Primary agent: <selected agent>
- Supporting/reviewer agents: <none or selected>
- Recommended model: <current/default or recommended alternative>
- Sisyphus: <not needed / recommended>
- Routing reason: <concise>

### Chunk completed
Name the one chunk executed.

### Files changed
Exact files, or `none`.

### Verification
Exact commands/checks and results.

### Drift / blockers
Unexpected state, approval gates, and `OUT OF SCOPE — NOT CHANGED`.

### Project-state sync
Canonical project docs updated at closeout, or `none` with reason.

### Next tiny chunk
Exactly one next chunk.

### STOP
End with:

**STOP — no next chunk was started.**
