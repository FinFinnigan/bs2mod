# dependencies
/node_modules

# next.js
/.next/
/out/

# opennext (cloudflare) build output
/.open-next/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# playwright-mcp test artifacts
/.playwright-mcp/

# local env files
.env
.env*.local

# keep the example template, ignore the real file
.env.example.local

# cloudflare local secrets (wrangler dev/deploy) — keep the example template
.dev.vars
.dev.vars.*
!.dev.vars.example

# typescript
*.tsbuildinfo
next-env.d.ts

# codegraph index (regenerable)
/.codegraph/

# OMO plan files (by design: .omo/ stays untracked)
/.omo/

# OMO prompt files — kept untracked awaiting a CEO decision
# (candidate: move to the Saleor Storefront repo)
0[1-4]-OMO-*.md
.neon

# QA / debugging artifacts (kept on disk locally, not part of the repo)
/artifacts/
/log
/browser-network.txt
/.debug-journal.md
/bs2mod-home-miski.png
/qa-desktop-1440.png
